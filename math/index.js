let x = 3.14;
let y = 5;
let z = 7;

x = Math.PI // mengeluarkan nilai PI tanpa menulisnya

// x = Math.round(x); // "ROUND" pembulatan nilai float ke atas
// x = Math.floor(x); // "FLOOR" pembulatan nilai float ke bawah
// x = Math.ceil(x); // "CEIL" pembulatan nilai float ke atas jika ada koma
// x = Math.pow(x, 2); // "POW" pemangkatan nilai / exponen
// x = Math.sqrt(x); // "SQRT" untuk mencari nilai akar kuadrat
// x = Math.abs(x); // "ABS" mengubah nilai agar tetap menjadi positif

maksimum = Math.max(x, y, z); // mencari nilai masksimum dari suatu nilai
minimum = Math.min(x, y, z); // mencari nilai minimum dari suatu nilai

console.log(minimum);